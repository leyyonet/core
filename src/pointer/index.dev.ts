const m = 'pointer:';
export const $devPointer = {
    bucket_invalid:         `${m}invalid.bucket`,
    bucket_duplicated:      `${m}duplicated.bucket`,

    name_duplicated:        `${m}duplicated.name`,

    value_duplicated:  `${m}value.duplicated`,
    value_invalid:          `${m}value.invalid`,
    value_absent:          `${m}value.absent`,

    primary_invalid:        `${m}primary.invalid`,
    primary_not_primary:    `${m}primary.not.primary`,
    primary_duplicated: `${m}primary.duplicated`,
    primary_is_alias:  `${m}primary.is.alias`,
    primary_absent:  `${m}primary.absent`,

    alias_not_supported:    `${m}alias.not.supported`,
    alias_invalid:          `${m}alias.invalid`,
    alias_is_primary:       `${m}alias.is.primary`,
    alias_duplicated:   `${m}alias.duplicated`,
    alias_absent:  `${m}alias.absent`,

};