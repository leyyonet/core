import {strict as assert} from 'assert';
import {cast1StringSimulate} from "./cast-1-string.simulate";

describe('cast1StringSimulate', () => {it('start', () => {assert.equal(true, true)})});
cast1StringSimulate(describe, it);