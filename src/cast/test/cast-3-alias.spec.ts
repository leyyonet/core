import {strict as assert} from 'assert';
import {cast3AliasSimulate} from "./cast-3-alias.simulate";

describe('cast3AliasSimulate', () => {it('start', () => {assert.equal(true, true)})});
cast3AliasSimulate(describe, it);