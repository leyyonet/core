/**
 * Extracts request data (param, query, body, header, file, context)
 * */
import {LY_CORE_NAME} from "../core";

export const LY_CONTEXT_H_REQUEST_EXTRACTOR = `${LY_CORE_NAME}_context:request_extractor`;
/**
 * Extracts request data (param, query, body, header, file, context)
 * */
export const LY_CONTEXT_H_SET = `${LY_CORE_NAME}_context:set`;
// noinspection JSUnusedGlobalSymbols
/**
 * Extracts context data
 * */
export const LY_CONTEXT_H_GET = `${LY_CORE_NAME}_context:get`; // todo
