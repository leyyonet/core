import {strict as assert} from 'assert';
import {generics4AliasSimulate} from "./generics-4-alias.simulate";

describe('generics4AliasSimulate', () => {it('start', () => {assert.equal(true, true)})});
generics4AliasSimulate(describe, it);