import {strict as assert} from 'assert';
import {generics2ArraySimulate} from "./generics-2-array.simulate";

describe('generics2ArraySimulate', () => {it('start', () => {assert.equal(true, true)})});
generics2ArraySimulate(describe, it);