import {LY_CORE_NAME} from "../core";

/**
 * Extracts context data which should be in subject
 * */
export const LY_LOGGER_H_BUILD_SUBJECT = `${LY_CORE_NAME}_logger:build_subject`;
export const LY_LOGGER_H_FORMAT = `${LY_CORE_NAME}_logger:format`;
/**
 * Print format of log line
 * */
export const LY_LOGGER_H_ADD = `${LY_CORE_NAME}_logger:add`;
